<<<<<<< HEAD
=======
version: "3.3"

>>>>>>> auth
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: /bin/bash -c "python manage.py runserver 0.0.0.0:8000"
    container_name: web-online-platform
    volumes:
      - .:/code
    env_file:
      - ./.env
    ports:
      - "8000:8000"
    depends_on:
<<<<<<< HEAD
      - db
      - elasticsearch
    networks:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://elasticsearch:9200"]
      interval: 10s
      retries: 5
=======
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - backend
    restart: always
>>>>>>> auth

  db:
    image: postgres:17-alpine
    container_name: db-postgres-platform
    env_file:
      - ./.env
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 4599
      POSTGRES_DB: streaming_platform
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5434:5433"
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always

  redis:
    image: redis:7
    container_name: redis-cache
    ports:
      - "6379:6379"
    networks:
      - backend
    restart: always

<<<<<<< HEAD
  elasticsearch:
    image: bitnami/elasticsearch:8.7.0
    container_name: elasticsearch-platform
    environment:
      - discovery.type=single-node
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    networks:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9200"]
      interval: 10s
      retries: 5
=======
  celery:
    build: .
    command: celery -A StreamingPlatform worker --loglevel=info
    container_name: celery-worker
    volumes:
      - .:/code
    env_file:
      - ./.env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      web:
        condition: service_started
    networks:
      - backend
    restart: always
>>>>>>> auth

volumes:
  postgres_data:
  elasticsearch_data:

networks:
  backend:
    driver: bridge
